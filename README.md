# x86-64-linux-Start-Init-Process
本專案整理  linux kernel 如何從 kernel initialization 過渡到 userspace 的 init process 的流程。主流 init process- Systemd 的概念與開機流程。研究如何利用 systemd 建立一個比 `multi-user.target` 更小、且能滿足應用服務需求的 system state。
